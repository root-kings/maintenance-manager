extends components/layout.pug

block styles

block navtitle
    a.teal-text.brand-logo(href="/") SPARES

block content
    //- .green.darken-2.hide-on-med-and-down
        .row
            .col.s12
                h4.header.light.white-text Dashboard
        
                      
    


    #machines
        

        .collection 
            a.collection-item(v-for="machine in machines" @click="viewMachine(machine._id)") {{machine.name}}
                i.material-icons.right chevron_right


block scripts
    script(src="/js/init/spares.js")
